{
  "name": "Finance (lightweight)",
  "install": "bash -lc 'set -e; if [ -f package.json ]; then if [ -f package-lock.json ]; then npm ci || npm install; else npm install; fi; else echo \"No package.json found; skipping npm install\"; fi'"
}
